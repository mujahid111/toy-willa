import { categories } from '../constants/site';
export function CategorySection() { return <section id="categories" className="section"><div className="section-head"><div><span className="eyebrow">SHOP BY FUN</span><h2>Find their next favorite toy</h2></div><p>From cuddly companions to creative challenges, there is something for every little personality.</p></div><div className="category-grid">{categories.map(([icon,name,desc])=><article className="category" key={name}><span className="category-icon">{icon}</span><h3>{name}</h3><p>{desc}</p><span className="arrow">→</span></article>)}</div></section>; }
